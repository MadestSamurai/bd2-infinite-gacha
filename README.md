# BD2 Infinite Gacha · 无限抽抽乐助手

> **免责声明：** 使用本辅助工具存在风险，可能导致账号处罚、封禁、游戏异常或数据损失。本项目不隶属于游戏官方，也不保证使用安全；请自行评估并遵守游戏相关规则，使用产生的风险及后果由用户自行承担。

[English](README.en.md) · 简体中文

[下载最新版本](https://github.com/MadestSamurai/bd2-infinite-gacha/releases/latest) · [问题反馈](https://github.com/MadestSamurai/bd2-infinite-gacha/issues)

适用于 BrownDust II Windows 客户端的独立无限抽抽乐助手。读取可抽服装与账号破度，按 A／B 目标和停止条件刷新、跳过动画，命中后保留结果供你确认。

## 下载

当前版本 **0.2.2**。两版功能相同，均内置简体中文／English。

| 版本 | 运行环境 | 建议 |
| --- | --- | --- |
| **Portable** | 内置 .NET 运行时 | 首次使用推荐，下载即用 |
| **Lite** | 需安装 [.NET Desktop Runtime 8 x64](https://dotnet.microsoft.com/download/dotnet/8.0) | 已安装运行时，下载更小 |

只需下载一种版本：EXE 可独立运行，ZIP 附中英文说明和许可证。无需 Python、开发 SDK 或其他 BD2 工具。Lite 需要 **Desktop Runtime**，基础 .NET Runtime 或 ASP.NET Runtime 不够。使用 `SHA256SUMS.txt` 校验下载。

## 快速开始

**升级前**：暂停并关闭旧工具，正常重启游戏，再打开新版连接。

1. 启动并登录游戏，进入无限抽抽乐，手动抽出第一次十连，停在结果页。
2. 打开助手，点「连接游戏」。升级过组件或本次游戏已连接其他工具时，请先正常重启游戏。
3. 点「未满服装 → B」，将未拥有及 +0～+4 的可抽服装加入左侧 B 列表。
4. 在 B 中选中优先目标，点「移入 A →」。Ctrl／Shift 可以多选；双击或 Enter 互移，Delete 移出分组。其他服装通过「添加服装…」选择。
5. 填写顶部 A 阈值，以及需要的低档位条件；点「开始刷新」。达标自动停止，最终保留／购买仍由你在游戏内决定。

## 功能与设置

### 卡池选择

卡池下拉框以**截止时间**区分活动，按新到旧排列；时间采用电脑本地时区，悬停可查看时区和游戏活动名。「当前结果」标记游戏正在显示的卡池。同一时间以卡池编号区分，游戏未提供日期的历史卡池排在末尾。调整排序不改变已有分组或停止条件。

### 停止条件

A 是优先目标，B 是补充目标，同一服装只能属于一组。顶部填写「A 至少 N 个」，范围 1～10；下方自动生成 N−1……0，各行可单独启用。

例如顶部填 3，再启用以下条件：

| 实际 A 数量 | 停止要求 |
| --- | --- |
| ≥ 3 | 无条件停止 |
| = 2 | B ≥ 3 |
| = 1 | B ≥ 5 |
| = 0 | 禁用，继续刷新 |

任何一条启用条件成立即可停止。低档位按 **A 数量相等**判断；A+B 不能大于10。非法或未完成输入会提示，但不会被重置。改变顶部阈值也保留隐藏档位的原输入。

- **重复服装按张数累计**：默认开启，同款每张分别计数；取消则按不同服装 ID 计数。
- **溢出服装不计**：按张数模式下默认开启，只计算升到 +5 所需张数。未拥有最多6张，+0最多5张，+4最多1张，+5不计。可单独取消。
- **刷新间隔**：默认1000ms，可调100～60000ms。上一次完整结果判定后才允许下一次；网络和游戏演出可能使实际间隔更长。
- 分组、条件按账号和卡池分别保存。停止或重开助手后不会自动开始刷新。

## 界面语言

在顶部 **语言 / Language** 中选择简体中文或 English。首次启动跟随系统：中文系统使用简体中文，其他系统使用英语；之后记住手动选择。切换语言不会重启任务或改变设置。游戏提供的名称和图片保持游戏语言，原始诊断保留原文。

维护翻译见 [翻译说明](docs/LOCALIZATION.md)。

## 兼容与限制

支持官方 Windows x64 PC 客户端，一次连接一个游戏进程；工具与游戏需使用相同权限。不支持手机或 Android 模拟器客户端。首次连接会解析本机接口并生成组件，可能需要数秒。无法可靠匹配接口时停止连接并显示原因；跨版本适配不保证所有未来更新都无需维护。

发行包不包含游戏 DLL、资源、账号库或私有采集文件。命中后只停止并保留当前结果，最终确认或购买由你在游戏内操作。

## 诊断与反馈

短暂读不到状态或界面时，最多等待15秒恢复；短暂文件占用会重试，保留当前任务。游戏弹窗关闭后自动继续。动画跳过使用游戏原生入口。

账号／进程／卡池变化、锁定结果和明确服务端错误仍会停止。服务器30秒未响应时不会盲目重发，避免跳过一份可能已经生成的结果。停止或关闭助手后不再发起新抽取，已发出的请求可正常返回。

本地数据位于 `%LOCALAPPDATA%\BD2InfiniteGacha\`：

| 文件 | 用途 |
| --- | --- |
| `settings.json` | 各账号、卡池的目标和条件 |
| `language.json` | 界面语言 |
| `desktop-events.jsonl`／`runtime-events.jsonl` | 恢复、停止和错误原因；自动限制体积 |
| `snapshot.json` | 最近库存、结果与动画状态 |
| `connection.json`／`compatibility.json`／`runtime.json` | 组件连接诊断 |

报告问题时提供工具版本、可见提示和事件日志相关几行即可。不要公开整份库存、账号数据或其他个人文件。

反馈时请提供版本、提示文字和相关日志片段，先去除账号、个人路径等信息。不要上传游戏 DLL、完整库存或连接凭据。

## 开发与贡献

需要 Windows x64、PowerShell 和 .NET 8 SDK。普通构建与回归不需要安装游戏，也不会连接游戏。

```powershell
.\build.ps1 -Locked
.\package.ps1 -Locked
```

产物位于 `dist/v版本号/`。打包会核对 Portable／Lite 运行环境并执行界面检查。

[开发与发布流程](docs/DEVELOPMENT.md) · [文档与发布格式](docs/PUBLICATION_STYLE.md) · [当前版本说明](docs/RELEASE_NOTES.md)

## 许可

项目代码采用 [MIT](LICENSE)。依赖保留各自许可，详见 [第三方许可说明](THIRD_PARTY_NOTICES.md)。本项目与游戏开发商或发行商无隶属关系。
